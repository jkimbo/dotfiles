# My dotfile


## Vim/Neovim setup

**Make sure neovim is installed**

Configuration:

```bash
ln -s ~/dotfiles/vim ~/.config/nvim
ln -s ~/dotfiles/vimrc ~/.config/nvim/init.vim
```

Install plugins:

```
nvim +PlugInstall
```
